export const CONFIG = {
  /**
   * Настройки LLM моделей
   */
  LLM: {
    /** Температура по умолчанию для обычных запросов */
    DEFAULT_TEMPERATURE: 0.6,
    /** Низкая температура для стабильных операций (суммаризация) */
    STABLE_TEMPERATURE: 0.3,
    /** Лимит токенов по умолчанию */
    DEFAULT_MAX_TOKENS: 2000,
    /** Лимит токенов для суммаризации */
    SUMMARY_MAX_TOKENS: 500,
    /** Лимит токенов для коротких ответов */
    SHORT_MAX_TOKENS: 100,
  },

  /**
   * Настройки сессий
   */
  SESSION: {
    /** Таймаут неактивной сессии (1 час) */
    TIMEOUT_MS: 60 * 60 * 1000,
    /** Интервал очистки старых сессий (15 минут) */
    CLEANUP_INTERVAL_MS: 15 * 60 * 1000,
    /** Включена ли компрессия по умолчанию */
    COMPRESSION_ENABLED: true,
    /** Порог сообщений для компрессии */
    COMPRESSION_THRESHOLD: 10,
  },

  /**
   * Настройки агентов
   */
  AGENT: {
    /** Максимальное количество итераций agent loop */
    MAX_ITERATIONS: 5,
    /** Температура для агентов */
    TEMPERATURE: 0.6,
    /** Лимит токенов для агентов */
    MAX_TOKENS: 2000,
  },

  /**
   * Настройки API
   */
  API: {
    /** Таймаут HTTP запросов (30 секунд) */
    REQUEST_TIMEOUT_MS: 30 * 1000,
    /** Количество повторных попыток при ошибке */
    MAX_RETRIES: 3,
  },

  /**
   * Настройки планировщика
   */
  SCHEDULER: {
    /** Интервал обновления прогнозов погоды (1 час) */
    WEATHER_UPDATE_INTERVAL_MS: 60 * 60 * 1000,
    /** Интервал очистки старых файлов (24 часа) */
    FILE_CLEANUP_INTERVAL_MS: 24 * 60 * 60 * 1000,
    /** Максимальный возраст файлов для очистки (7 дней) */
    MAX_FILE_AGE_MS: 7 * 24 * 60 * 60 * 1000,
  },

  /**
   * Настройки логирования
   */
  LOGGING: {
    /** Включить детальное логирование */
    VERBOSE: process.env.LOG_VERBOSE === "true",
    /** Уровень логирования */
    LEVEL: process.env.LOG_LEVEL || "info",
  },
} as const;
